
module.exports = {
    justOnePlease: (i) => {
        return i === 0 ? 'active':'';
    },
    // loggedInFalse: (loggedIn) => {
    //     if (loggedIn === true) {
    //         return false;
    //     }
    // }
};

